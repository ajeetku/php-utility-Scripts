# php-utility-Scripts
Some PHP Use full Scripts

--------------------------------------------------------
Uses of timezone conversion.php
convert('10:30 PM');
We can set timezone:
$tz_from = <BASE_TIMEZONE>;
$tz_to = <CONVERT_TIMEZONE>;
--------------------------------------------------------
